apiVersion: 1

datasources:
  - name: InfluxDB
    uid: efifgp8jvgj5sf
    type: influxdb
    access: proxy
    url: http://localhost:8086
    isDefault: true
    editable: true
    jsonData:
      defaultBucket: midnight_rider
      organization: MidnightRider
      version: Flux
    secureJsonData:
      token: ${INFLUX_TOKEN}
