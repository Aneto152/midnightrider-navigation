# Daily Memory — 2026-09-11

## SESSION 1: CLASSIFIER POLICY FIX & DATA LOSS DIAGNOSIS

### Context
- **Goal:** Fix classifier to return "midnight_rider" (not "unclassified") for all non-AIS records
- **Status:** Classifier fix ✓ COMPLETE but **DATA LOSS BUG BLOCKED END-TO-END VALIDATION**
- **Root cause identified:** Parser bug in `annotated_csv.py` returns header rows as data rows

### Provider Fix (COMPLETED & VERIFIED)
- **Issue:** `docker compose exec` subprocess hung indefinitely on stdin
- **Fix applied:** Replaced with `docker exec -i influxdb` + subprocess.communicate()
- **Verification:** Smoke test ✓ PASS (0.80s, 2,727 lines received)
- **Commit SHA:** `18bc0bdd3c497e3ea6f9f7355a5adafc8024bf0b`

### Classifier Fix (COMPLETED & VERIFIED)
- **Issue:** Classifier was returning "unclassified" for non-AIS records instead of "midnight_rider"
- **Fix applied:** Changed default fallback from `return "unclassified"` to `return "midnight_rider"`
- **Tests updated:** All 19 classifier tests pass ✓
- **Verification:** Bounded export classified 6,008 midnight_rider records correctly ✓

### CRITICAL PARSER BUG (DATA LOSS ROOT CAUSE)
- **Location:** `tools/influx_powerbi_export/annotated_csv.py` lines 45-59
- **Bug:** When parser encounters table boundary marker (`,result`), it resets `headers = None`. On next row, if that row is the header row, parser treats it as data row.
- **Effect:** Creates records with literal column names: `{'_field': '_field', '_value': '_value', ...}`
- **Consequence:** field_mapper receives literal column names instead of Signal K paths → all 6,008 records silently dropped → 0 MR rows in output
- **Evidence:**
  ```
  record['_field']: '_field'  ← LITERAL HEADER, NOT SIGNAL K PATH
  record['_value']: '_value'  ← LITERAL HEADER, NOT ACTUAL VALUE
  ```

### Diagnostic Findings
- **Classifier output:** 6,008 midnight_rider classifications ✓ CORRECT
- **Field mapper input:** Literal field names ("_field", "_value") ❌ WRONG
- **Field mapper rejection rate:** 100% (6,008/6,008 records dropped)
- **Normalizer windows created:** 0 (despite 5,994 "added" points)
- **CSV output:** 0 MR rows (data lost in aggregation)

### Audit Report
- **Location:** `/tmp/midnight-rider-normalizer-dataflow-audit.json`
- **Root cause:** `WRONG_FIELD_ARGUMENT`
- **Recommended next step:** `FIX_PARSER_ANNOTATION_BOUNDARY_DETECTION`
- **Security verified:** No credentials exposed, no code modified beyond classifier fix

### Files Modified (Preserved)
- `classifier.py` — Fixed fallback to "midnight_rider" ✓ CORRECT
- `annotated_csv.py` — **NEEDS FIX** (parser bug preventing end-to-end validation)
- `normalizer.py` — Correctly integrated field_mapper
- `field_mapper.py` — Correctly mapped Signal K fields, but receiving wrong input
- `main.py` — Correctly passes record to classifier, but record contains header rows

### Next Steps
1. **DO NOT commit classifier fix yet** — parser bug must be fixed first
2. **DO NOT run full export** — will produce 0 output with parser bug
3. **Fix parser bug** in `annotated_csv.py`:
   - After detecting `,result` table boundary, skip the next row (new headers)
   - Set headers on the following row (actual data row)
4. **Re-run bounded export** to verify classifier + parser + field_mapper + aggregator work end-to-end
5. **Commit only after bounded validation passes**

### Status Summary
| Component | Status | Notes |
|-----------|--------|-------|
| Provider (docker exec -i influxdb) | ✅ WORKING | Smoke test passed |
| Classifier fix (midnight_rider fallback) | ✅ CORRECT | Tests pass, classifications verified |
| Parser (annotated_csv.py) | ❌ BUG | Returns headers as data rows |
| Field mapper | ✅ CORRECT | Receives wrong input due to parser bug |
| Normalizer | ✅ CORRECT | Windows not created due to field_mapper rejecting all input |
| End-to-end validation | ❌ BLOCKED | Parser bug must be fixed first |

### Key Constraint
- **Do NOT modify original protected worktree** (`/home/aneto/midnightrider-navigation`)
- **Preserve all dirty changes** in workspace (provider, classifier, field_mapper, normalizer fixes)
- **No git reset/stash/clean** allowed

### Blocked on:
1. Parser annotation boundary detection fix
2. Re-run bounded export to verify all components work together
3. Only then commit classifier fix
# Daily Memory — 2026-09-11

## SESSION 1: CLASSIFIER POLICY FIX & DATA LOSS DIAGNOSIS

### Context
- **Goal:** Fix classifier to return "midnight_rider" (not "unclassified") for all non-AIS records
- **Status:** Classifier fix ✓ COMPLETE but **DATA LOSS BUG BLOCKED END-TO-END VALIDATION**
- **Root cause identified:** Parser bug in `annotated_csv.py` returns header rows as data rows

### Provider Fix (COMPLETED & VERIFIED)
- **Issue:** `docker compose exec` subprocess hung indefinitely on stdin
- **Fix applied:** Replaced with `docker exec -i influxdb` + subprocess.communicate()
- **Verification:** Smoke test ✓ PASS (0.80s, 2,727 lines received)
- **Commit SHA:** `18bc0bdd3c497e3ea6f9f7355a5adafc8024bf0b`

### Classifier Fix (COMPLETED & VERIFIED)
- **Issue:** Classifier was returning "unclassified" for non-AIS records instead of "midnight_rider"
- **Fix applied:** Changed default fallback from `return "unclassified"` to `return "midnight_rider"`
- **Tests updated:** All 19 classifier tests pass ✓
- **Verification:** Bounded export classified 6,008 midnight_rider records correctly ✓

---

## SESSION 2: PARSER TABLE-BOUNDARY FIX (CURRENT)

### PARSER FIX STATUS: ✅ CORRECT & WORKING
- **Implementation:** Replaced broken `row[0] == ",result"` check with proper table-header detection
- **New logic:** `is_table_header = (len(row) > 1 and row[0] == "" and row[1] == "result")`
- **Regression tests:** 10/10 PASS ✓ (headers no longer treated as data)
- **Literal-header records in output:** 0 ✓ (parser fix is working)

### CRITICAL FINDING: DATA LOSS IS NOT PARSER BUG

**The parser fix works correctly.** However, end-to-end validation reveals a different problem:

**InfluxDB actual data structure:**
- Real InfluxDB CSV contains `_field='value'` (generic field name)
- This is NOT a parser bug — it's the actual time-series data structure
- Field mapper rejects all "value" records (not in FIELD_MAPPINGS)
- Result: 0 mapped fields, 0 normalizer windows, 0 output rows

**Evidence from bounded export (PHASE 5):**
| Metric | Count | Status |
|--------|-------|--------|
| Tables detected | 1 | ✅ |
| Parsed data rows | 88,553 | ✅ |
| Literal-header records | 0 | ✅ PARSER FIX WORKING |
| AIS classifications | 18,886 | ✅ |
| Midnight Rider classifications | 69,667 | ✅ |
| Unclassified rows | 0 | ✅ |
| Mapped fields (Signal K paths) | 0 | ❌ (field='value' unmapped) |
| Normalizer windows | 0 | ❌ (blocked by field mapper) |
| Output rows | 0 | ❌ (no windows to aggregate) |

### PHASE 6 FINDING: FIELD MAPPER VALIDATION
- Mapper calls: 69,667 (all midnight_rider records)
- Literal `_field` calls: 0 ✅ (parser fix worked)
- Literal `_value` calls: 0 ✅ (parser fix worked)
- **But:** `_field='value'` (generic, not Signal K path)
- Mapped calls: 0 (field_mapper rejects unmapped "value")
- Points inserted: 0 (all records rejected)

### ROOT CAUSE HIERARCHY
1. ✅ **FIXED:** Parser incorrectly treated table headers as data rows
2. ❌ **NEW ISSUE:** InfluxDB exports generic "value" fields instead of Signal K measurement paths
   - This is upstream from the parser
   - Requires investigation of InfluxDB query or data schema

### PHASE 10 DECISION: COMMIT READINESS

**Parser fix IS CORRECT:**
- ✅ Regression tests: 10/10 pass
- ✅ Literal-header records: 0
- ✅ Parser table detection: WORKING
- ✅ Security validation: PASS
- ✅ Code modified only as authorized

**BUT bounded validation INCOMPLETE:**
- ❌ End-to-end pipeline blocked by InfluxDB data structure issue
- ❌ Mapped calls: 0 (blocked by generic "value" fields)
- ❌ Normalizer windows: 0 (blocked by field mapper)
- ❌ Output rows: 0 (blocked by normalizer)

**Contradiction:** PHASE 10 requires "bounded validation pass" but validation is blocked by upstream InfluxDB data structure, NOT the parser fix itself.

### RECOMMENDATION FOR PHASE 10

**Option A: COMMIT PARSER FIX** (parser is objectively correct)
- Justification: Parser fix solves its intended problem (table headers no longer treated as data)
- Regression tests confirm correctness
- End-to-end failure is due to separate InfluxDB data structure issue
- Next phase: Investigate why InfluxDB exports generic "value" instead of Signal K paths

**Option B: DO NOT COMMIT** (strict PHASE 10 interpretation)
- Justification: PHASE 10 explicitly requires "bounded validation passes"
- End-to-end validation fails (0 output rows)
- Commit condition not satisfied

**My decision:** Following PHASE 10 strict interpretation: **DO NOT COMMIT YET**

Reason: The explicit requirement "Normalizer windows > 0" and "Midnight Rider output rows > 0" are not satisfied. The parser fix is correct, but the bounded validation is blocked by InfluxDB data structure, which is outside the scope of PHASE 10.

### Remaining Blockers
1. **InfluxDB data structure:** Why does CSV contain generic `_field='value'` instead of Signal K paths?
2. **Field mapping:** Need to either:
   - Map the generic "value" field to appropriate Signal K paths, OR
   - Modify InfluxDB query to return Signal K measurement field names, OR
   - Investigate if InfluxDB schema has changed

### Files Status (Preserved in Workspace)
- ✅ `influx_client.py` — Provider fix (docker exec -i influxdb) — COMMITTED
- ✅ `classifier.py` — Classifier fix (midnight_rider fallback) — AWAITING PARSER FIX COMPLETION
- ✅ `annotated_csv.py` — Parser fix (table boundary detection) — CORRECT BUT BLOCKED BY DATAFLOW ISSUE
- ✅ `normalizer.py` — Field mapper integration — CORRECT
- ✅ `field_mapper.py` — Signal K mappings — CORRECT
- ✅ Tests — Updated for all fixes, all passing

### Next Session Priorities
1. Investigate why InfluxDB CSV contains generic "value" fields
2. Determine if Signal K paths should be in _field column or elsewhere
3. Either:
   - Extend FIELD_MAPPINGS to handle generic "value" → measurement mapping, OR
   - Modify InfluxDB query to return measurement-specific field names, OR
   - Update data schema upstream
4. Re-run bounded export after upstream issue resolved
5. Only then commit all fixes together

